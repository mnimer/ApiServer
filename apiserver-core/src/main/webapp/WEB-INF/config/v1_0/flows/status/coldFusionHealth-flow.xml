<?xml version="1.0" encoding="UTF-8"?>
<beans
        xmlns:http="http://www.springframework.org/schema/integration/http"
        xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:si="http://www.springframework.org/schema/integration"
        xmlns:task="http://www.springframework.org/schema/task" xmlns:util="http://www.springframework.org/schema/util"
        xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration/http http://www.springframework.org/schema/integration/http/spring-integration-http.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <util:list id="messageConverters">
        <bean class="org.springframework.http.converter.ResourceHttpMessageConverter"/>
        <bean class="org.springframework.http.converter.FormHttpMessageConverter"/>
        <bean class="org.springframework.http.converter.StringHttpMessageConverter"/>
    </util:list>

    <task:executor id="coldFusionHealthTaskExecutor" pool-size="100" />

    <si:channel id="coldFusionHealthRequestChannel">
        <si:queue/>
        <si:interceptors>
            <si:wire-tap channel="logger"/>
        </si:interceptors>
    </si:channel>

    <si:channel id="coldFusionHealthReplyChannel">
        <si:queue/>
        <si:interceptors>
            <si:wire-tap channel="logger"/>
        </si:interceptors>
    </si:channel>

    <si:channel id="coldFusionHealthHTTPChannel">
        <si:dispatcher task-executor="coldFusionHealthTaskExecutor"/>
        <si:interceptors>
            <si:wire-tap channel="logger"/>
        </si:interceptors>
    </si:channel>



    <si:gateway id="coldFusionHealthApiGateway"
                service-interface="apiserver.core.gateways.ApiStatusGateway"
                default-reply-channel="coldFusionHealthReplyChannel"
                default-reply-timeout="#{applicationProperties.defaultReplyTimeout}">

        <si:method name="checkColdfusionSync"
                   request-channel="coldFusionHealthRequestChannel"
                   payload-expression="new java.util.Date().toGMTString()">  <!-- add a fake payload-expression, spring doesn't like empty payloads for http requests -->
            <si:header name="REQUEST_TYPE" value="#{coldfusionProperties.REQUEST_TYPE}"/>
        </si:method>

        <si:method name="checkColdfusionAsync"
                   request-channel="coldFusionHealthRequestChannel"
                   payload-expression="new java.util.Date().toGMTString()">  <!-- add a fake payload-expression, spring doesn't like empty payloads for http requests -->
            <si:header name="REQUEST_TYPE" value="#{coldfusionProperties.REQUEST_TYPE}"/>
        </si:method>
    </si:gateway>


    <si:header-value-router input-channel="coldFusionHealthRequestChannel"
                            header-name="REQUEST_TYPE">
        <si:poller fixed-rate="100" max-messages-per-poll="10" task-executor="coldFusionHealthTaskExecutor"/>
        <si:mapping value="http" channel="coldFusionHealthHTTPChannel"/>
    </si:header-value-router>


    <si:chain id="trafficGatewayChain"
              input-channel="coldFusionHealthHTTPChannel"
              output-channel="coldFusionHealthReplyChannel">
        <http:outbound-gateway
                url-expression="'http://#{coldfusionProperties.host}:#{coldfusionProperties.port}/#{coldfusionProperties.path}/api-status.cfc?method=health'"
                http-method="GET" reply-timeout="10000"
                extract-request-payload="false"
                expected-response-type="java.lang.String"
                message-converters="messageConverters">
        </http:outbound-gateway>

        <si:json-to-object-transformer type="java.util.HashMap"/>
    </si:chain>




</beans>